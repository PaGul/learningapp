<%provide(:title, "All users")%>
<h1>All users</h1>
<%=will_paginate %>  <!--показывает ссылки на страницы пользователей -->
<ul class="users" >
	<%= render @users %> <!-- вызывает последовательно _user.html.erb -->
</ul>
<%=will_paginate %>
		